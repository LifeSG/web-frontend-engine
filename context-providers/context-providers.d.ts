import { ReactElement } from "react";
interface IProps {
    children: ReactElement;
}
export declare const ContextProviders: ({ children }: IProps) => import("react/jsx-runtime").JSX.Element;
export {};
